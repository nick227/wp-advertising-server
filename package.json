{
  "name": "wp-ad-community-service",
  "version": "4.0.0",
  "description": "Ultra-light Community Ads microservice for WP Advertising plugin with final operations hardening.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build:plugin": "node scripts/build-plugin-zip.mjs",
    "build": "npm run build:plugin && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "lint": "eslint src tests",
    "test": "vitest run",
    "test:billing:mysql": "node scripts/test-billing-mysql.mjs",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check": "npm run typecheck && npm run lint && npm run test",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "openapi:validate": "node scripts/validate-openapi.mjs",
    "smoke": "node scripts/smoke-test.mjs",
    "seed:soft-launch": "tsx scripts/seed-soft-launch.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "nanoid": "^5.0.7",
    "stripe": "^17.7.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "@types/supertest": "^6.0.2",
    "@vitest/coverage-v8": "^2.1.0",
    "adm-zip": "^0.6.0",
    "eslint": "^9.5.0",
    "prisma": "^6.8.2",
    "supertest": "^7.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.0.0",
    "vitest": "^2.1.0",
    "yaml": "^2.6.1"
  },
  "engines": {
    "node": ">=20"
  }
}
