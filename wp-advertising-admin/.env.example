AD_SERVER_BASE_URL="http://localhost:4100/v1"
PORT=4200
NODE_ENV=development
# Optional: pre-seed cookie auth in local only by skipping login (not for production).
# SESSION_SECRET used to sign the operator token cookie.
SESSION_SECRET="change-me-admin-session-secret-min-32-chars"
